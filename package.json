{
  "name": "axonai",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start": "vite preview --host --port $PORT"
  },
  "dependencies": {
    "recharts": "^2.15.3"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  },
  "engines": {
    "node": "18.x"
  }
}